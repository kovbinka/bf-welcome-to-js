'use strict';

/* Booleans

TODO: create a few variables(boolean):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/


// DECLARE a variable named `isDaytime`
// INITIALIZE the value to `true`
let isDaytime = true;

// DECLARE a variable named `isRaining`
// INITIALIZE the value to `false`
let isRaining = false;

// READ the value stored in `isDaytime`
console.log(isDaytime);

// READ the value stored in `isRaining`
console.log(isRaining);

// ASSIGN a new value to `isDaytime`
isDaytime = false;

// ASSIGN a new value to `isRaining`
isRaining = true;

// READ the value stored in `isDaytime`
console.log(isDaytime);

// READ the value stored in `isRaining`
console.log(isRaining);



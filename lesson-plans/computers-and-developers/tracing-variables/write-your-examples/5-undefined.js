'use strict';

/* Undefined

TODO: create a few variables(undefined):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/


// DECLARE a variable named `unknownValue`
// It is declared but not initialized (undefined)
let unknownValue;

// DECLARE a variable named `notSetYet`
// It is declared but not initialized (undefined)
let notSetYet;

// READ the value stored in `unknownValue`
console.log(unknownValue);

// READ the value stored in `notSetYet`
console.log(notSetYet);

// ASSIGN a new value to `unknownValue`
unknownValue = "Now defined";

// ASSIGN a new value to `notSetYet`
notSetYet = 42;

// READ the value stored in `unknownValue`
console.log(unknownValue);

// READ the value stored in `notSetYet`
console.log(notSetYet);
'use strict';

/* Null

TODO: create a few variables(null):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/


// DECLARE a variable named `userName`
// INITIALIZE the value to `null`
let userName = null;

// DECLARE a variable named `response`
// INITIALIZE the value to `null`
let response = null;

// READ the value stored in `userName`
console.log(userName);

// READ the value stored in `response`
console.log(response);

// ASSIGN a new value to `userName`
userName = "Alice";

// ASSIGN a new value to `response`
response = "Success";

// READ the value stored in `userName`
console.log(userName);

// READ the value stored in `response`
console.log(response);